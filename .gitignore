# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
.env

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Superpowers specs/plans + SDD scratch (kept local, not tracked)
docs/superpowers/
.superpowers/

# Local scratch / source material
tmp-src/

# Generated by `bun run ingest` / `bun run author` (regenerate locally; not tracked).
# Downloaded press images and non-deterministic daily editions stay out of git.
content/sources/
content/src/
content/img/cnn-*
content/img/fox-*
content/img/????-??-??/

# CI archive scratch (state lives in Azure Blob, not git)
/archive/
*.tar.gz

# Daily market snapshot — restored from Azure Blob, not committed
content/market.json

# Weekly traffic report — persisted to the analytics Azure Blob container, not
# committed. A local run must never re-commit this machine-generated log.
docs/traffic-log.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
